<p id="notice"><%= notice %></p>
単語帳(クリックすると表裏が入れ替わります)
<div class="wordbook--top">
  <% @book.questions.each do |q| %>
    <div class="card__object">
      <div class="card__cell">
        <div class="wordbook__tail">
          <div class="wordbook__tail--content">
            <div class="wordbook__text", style="color: <%= q.cont_color %>; font-size: <%= @book.font_size %>px"><%= q.content %></div>
          </div>
        </div>
        <div class="wordbook__head">
          <div class="wordbook__head--content">
            <div class="wordbook__text", style="color: <%= q.ans_color %>; font-size: <%= @book.font_size %>px"><%= q.answer %></div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
<input type="button" value="シャッフル" id="wordbook_shuffle"><br />